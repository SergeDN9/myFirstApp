import { LightOnOverDirective } from './light-on-over.directive';

describe('LightOnOverDirective', () => {
  it('should create an instance', () => {
    const directive = new LightOnOverDirective();
    expect(directive).toBeTruthy();
  });
});
